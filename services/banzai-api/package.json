{
  "name": "@banza/banzai-api",
  "version": "0.1.0",
  "private": true,
  "description": "BanzAI knowledge API — subordinate, non-authoritative. Explains protocol rules and cites sources; never certifies, decides, or substitutes the conformance suite. Runs offline with a deterministic mock provider (no external LLM, no GPU, no model downloads).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "eval": "node eval/gen-m2-18b6-dataset.mjs --check && node eval/run-m2-18b6-eval.mjs && node eval/parity-m2-18b6.mjs",
    "eval:gen": "node eval/gen-m2-18b6-dataset.mjs",
    "eval:live": "node eval/run-m2-18b6-live.mjs",
    "eval:canonical": "node eval/gen-canonical-eval.mjs --check && node eval/canonical-reconciliation.mjs --check && node eval/canonical-metrics.mjs --check",
    "eval:canonical:gen": "node eval/gen-canonical-eval.mjs && node eval/canonical-reconciliation.mjs && node eval/canonical-metrics.mjs"
  },
  "dependencies": {
    "pg": "8.13.1"
  }
}

# Operador Zero — Premium Read-Only UI (M2.19E/F)

**Status:** built + verified. Branch `release/m2-19ef-premium-operator-zero-banzai-validation`.

The interactive lab (`OperadorZeroLab`, 668 lines) was deleted and replaced by `OperadorZeroReference.tsx`
— a premium, dark-first, WCAG-AA, responsive **read-only** surface with: hero (eyebrow/lede/CTAs),
status strip, implementation identity, **technical status as categorical states (no score)**, capabilities,
proof chain, artifact explorer (view/open/copy — read-only), validate-in-BanzAI section, institutional
boundary, FAQ. Certification shown honestly as **NOT_CERTIFIED**. `next build`: `/oz` 8.85 kB static.
No local execution, no mutable ledger, no simulation controls (guard-enforced).
